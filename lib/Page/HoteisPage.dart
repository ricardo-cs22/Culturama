import 'package:flutter/material.dart';

class HoteisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hotéis'),
      ),
      body: Center(
        child: Text('Conteúdo dos Hotéis'),
      ),
    );
  }
}