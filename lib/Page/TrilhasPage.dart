import 'package:flutter/material.dart';

class TrilhasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trilhas'),
      ),
      body: Center(
        child: Text('Conteúdo das Trilhas'),
      ),
    );
  }
}