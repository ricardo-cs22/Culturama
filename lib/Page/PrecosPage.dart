import 'package:flutter/material.dart';

class PrecosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Preços'),
      ),
      body: Center(
        child: Text('Conteúdo dos Preços'),
      ),
    );
  }
}